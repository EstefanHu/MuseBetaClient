import React from 'react';

const styles = {
  container: {
    backgroundColor: 'white',
    width: '500px',
    height: '100vh',
  }
}

export const Feed = () => {
  return (
    <section style={styles.container}>
      
    </section>
  )
}